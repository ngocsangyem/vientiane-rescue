import Section12Component from "../section-12.component";
describe("Section112Component View", function() {
	beforeEach(() => {
		this.section12 = new Section12Component();
	});

	it("Should run a few assertions", () => {
		expect(this.section112).to.exist;
	});
});
