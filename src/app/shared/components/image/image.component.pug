mixin image(spec)
	- spec = spec || {}
	- spec.class = spec.class || ''
	- spec.src = spec.src || ''
	- spec.alt = spec.alt || 'Our Better World'
	img(class=spec.class src=spec.src, alt=spec.alt)&attributes(attributes)
